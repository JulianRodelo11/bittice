
version: "3.9"
services:
  engine:
    image: bittice-sakila
    container_name: bittice-sakila-engine
    ports:
      - "3000:3000"
      - "50051:50051"
    command: ["./bittice", "server", "--type", "all"]
    environment:
      - BITTICE_HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always

  sync:
    image: bittice-sakila
    container_name: bittice-sakila-sync
    command: ["./bittice", "cdc", "--url", "mysql://root:p_ssW0rd@host.docker.internal:3306/sakila", "--entity", "sakila", "--database", "sakila"]
    volumes:
      - ./data:/app/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
